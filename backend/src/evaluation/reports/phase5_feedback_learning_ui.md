# Phase 5: 사용자 피드백 학습 + Frontend UI 개선 보고서

## 1. 개요

### 1.1 고도화 배경
Phase 1~4를 통해 이메일 분류 정확도가 95%에 도달하였으나, 실제 사용자 경험과 시스템의 지속적 학습 능력 부족이 확인됨.

| Phase | 내용 | 정확도 |
|-------|------|--------|
| Phase 1 | Baseline (Gemini only) | 79.4% |
| Phase 2 | + RAG (ChromaDB) | 79.2% |
| Phase 3 | + Prompt Engineering | 95.0% |
| Phase 4 | + Advanced RAG (4 techniques) | 93.6% |
| **Phase 5** | **+ 피드백 학습 + UI 개선** | **-** |

> Phase 5는 정확도 향상이 아닌 **기능적 고도화**에 집중

### 1.2 고도화 목표
1. **사용자 피드백 학습 시스템**: AI가 사용자의 답변 스타일을 학습하여 개인화된 답변 생성
2. **Frontend UI 개선**: 통계 대시보드 및 답변 히스토리 기능 추가

---

## 2. 구현 상세

### 2.1 사용자 피드백 학습 시스템

#### 2.1.1 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    피드백 학습 플로우                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [1] AI 답변 생성                                                │
│       │                                                         │
│       ▼                                                         │
│  [2] 사용자 검토/수정                                            │
│       │                                                         │
│       ▼                                                         │
│  [3] 답변 발송 ──────────────────────────────────────┐          │
│       │                                              │          │
│       ▼                                              ▼          │
│  [4] 피드백 저장 (ChromaDB)                    [5] 이메일 발송    │
│       │                                                         │
│       ▼                                                         │
│  [6] 향후 답변 생성 시 유사 피드백 참조                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.1.2 데이터 구조

**ChromaDB Collection: `reply_templates`**

```python
{
    "id": "feedback_{email_id}_{timestamp}",
    "document": "{email_subject}\n{email_body}\n\n답변:\n{final_reply}",
    "metadata": {
        "email_id": int,
        "email_type": str,        # "개인", "공지", "마케팅" 등
        "selected_tone": str,     # "formal", "casual", "brief"
        "was_modified": bool,     # 사용자 수정 여부
        "reply_text": str,        # 최종 발송된 답변
        "source": "user_feedback"
    }
}
```

#### 2.1.3 핵심 메서드

**`backend/src/rag/rag_service.py`**

| 메서드 | 기능 | 파라미터 |
|--------|------|----------|
| `add_user_feedback()` | 피드백을 ChromaDB에 저장 | email_id, email_subject, email_body, email_type, original_draft, final_reply, selected_tone, was_modified |
| `get_feedback_enhanced_reply_prompt()` | 피드백 기반 프롬프트 생성 | email_subject, email_body, email_type, sender_name, preferred_tone, n_feedback_examples |
| `_search_feedback_examples()` | 유사 피드백 검색 | query_text, email_type, n_results |
| `get_feedback_statistics()` | 피드백 통계 조회 | - |

#### 2.1.4 학습 알고리즘

```python
# 피드백 저장 로직
def add_user_feedback(...):
    # 1. 수정 여부 판단
    was_modified = (original_draft.strip() != final_reply.strip())

    # 2. 임베딩용 텍스트 생성 (이메일 + 답변 결합)
    combined_text = f"{email_subject}\n{email_body}\n\n답변:\n{final_reply}"

    # 3. ChromaDB에 벡터화하여 저장
    self.reply_collection.add(
        documents=[combined_text],
        metadatas=[metadata],
        ids=[f"feedback_{email_id}_{timestamp}"]
    )
```

```python
# 피드백 활용 로직
def get_feedback_enhanced_reply_prompt(...):
    # 1. 유사 피드백 검색 (이메일 내용 기반)
    similar_feedbacks = self._search_feedback_examples(
        query_text=f"{email_subject}\n{email_body}",
        email_type=email_type,
        n_results=n_feedback_examples
    )

    # 2. 프롬프트에 참조 답변으로 추가
    if similar_feedbacks:
        prompt += "\n\n[참고 - 사용자가 선호하는 답변 스타일]:\n"
        for feedback in similar_feedbacks:
            prompt += f"- {feedback['reply_text'][:200]}...\n"
```

### 2.2 Backend API 추가

**`backend/src/main.py`**

| Endpoint | Method | 기능 |
|----------|--------|------|
| `/stats/overview` | GET | 전체 이메일 통계 조회 |
| `/stats/reply-history` | GET | 답변 히스토리 페이지네이션 조회 |
| `/feedback/learn` | POST | 피드백 학습 (ChromaDB 저장) |
| `/feedback/stats` | GET | 피드백 학습 통계 조회 |

#### API 응답 예시

**GET `/stats/overview`**
```json
{
    "email_stats": {
        "total": 13,
        "analyzed": 4,
        "replied": 1,
        "pending_reply": 0
    },
    "type_distribution": {
        "공지": 2,
        "기타": 1,
        "개인": 1
    },
    "importance_distribution": {
        "high": 2,
        "low": 1,
        "medium": 1
    },
    "sentiment_distribution": {
        "neutral": 1,
        "positive": 3
    },
    "daily_emails": [...],
    "reply_stats": {
        "total_sent": 1,
        "modified_by_user": 0
    }
}
```

**GET `/feedback/stats`**
```json
{
    "success": true,
    "total_feedback": 1,
    "accepted_count": 0,
    "modified_count": 1,
    "modification_rate": 100.0,
    "by_tone": {
        "formal": 1
    },
    "by_type": {
        "개인": 1
    }
}
```

### 2.3 Frontend UI 개선

#### 2.3.1 새로운 컴포넌트

| 컴포넌트 | 파일 | 기능 |
|----------|------|------|
| StatsPanel | `StatsPanel.js` | 통계 대시보드 (3탭) |
| ReplyHistory | `ReplyHistory.js` | 답변 히스토리 조회 |

#### 2.3.2 StatsPanel 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                    📊 통계 대시보드                              │
├─────────────────────────────────────────────────────────────────┤
│  [개요]  [분포]  [학습 현황]                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  개요 탭:                                                       │
│  ┌──────────┬──────────┬──────────┬──────────┐                 │
│  │ 📧 전체  │ 🔍 분석  │ ✅ 답변  │ ⏳ 대기  │                 │
│  │   13     │    4     │    1     │    0     │                 │
│  └──────────┴──────────┴──────────┴──────────┘                 │
│                                                                 │
│  [최근 7일 이메일 수신 차트]                                     │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  분포 탭:                                                       │
│  - 📁 유형별 분포 (공지 50%, 기타 25%, 개인 25%)                 │
│  - ⚡ 중요도별 분포 (높음/보통/낮음)                             │
│  - 😊 감정별 분포 (긍정/중립/부정)                               │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  학습 현황 탭:                                                   │
│  - 🧠 AI 학습 현황: "N개의 답변 패턴을 학습했습니다"             │
│  - 그대로 승인 / 수정 후 발송 / 수정률                           │
│  - 톤별 학습 데이터                                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.3.3 ReplyHistory 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                    📬 답변 히스토리                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ user@example.com              11월 26일 오전 08:57      │   │
│  │ Re: 테스트 이메일                                        │   │
│  │ 안녕하세요, 홍길동님. 보내주신 메일 잘 받아보았습...      │   │
│  │ [개인] 중요도: 4/10                                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  [← 이전]  1 / 1 페이지  [다음 →]                               │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  상세 모달:                                                     │
│  - 받는 사람: 이름 <이메일>                                     │
│  - 제목: Re: ...                                                │
│  - 발송 시간: 2025-11-26 08:57:56                               │
│  - 상태: ✅ 원본 그대로 발송 / ✏️ 수정 후 발송                   │
│  - 답변 내용: [전체 텍스트]                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.3.4 Dashboard 헤더 업데이트

```
┌─────────────────────────────────────────────────────────────────────────┐
│  AI 메일 비서                                                           │
│  LangGraph + Gemini 기반 이메일 자동화 시스템                            │
│                                                                         │
│  [📧 메일 동기화] [📝 일일 요약] [📊 요약 보기] [📈 통계] [📬 답변 히스토리] │
│                                                  ↑ NEW    ↑ NEW         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.3.5 ReplyGenerator 피드백 통합

```javascript
// frontend/src/components/ReplyGenerator.js (Line 104-118)
const handleSendReply = async () => {
    // 1. 답변 발송
    await sendReply(email.id, editedReply, email.sender_address, email.sender_name);

    // 2. 피드백 학습 (백그라운드)
    if (selectedDraft) {
        await learnFromFeedback(
            email.id,
            selectedDraft.content,  // 원본 AI 답변
            editedReply,            // 최종 발송 답변
            selectedDraft.tone      // 선택한 톤
        );
    }
};
```

---

## 3. 파일 변경 내역

### 3.1 Backend

| 파일 | 변경 내용 | 추가 라인 |
|------|----------|----------|
| `backend/src/rag/rag_service.py` | 피드백 학습 메서드 4개 추가 | ~270 lines |
| `backend/src/main.py` | 통계/피드백 API 엔드포인트 추가 | ~300 lines |

### 3.2 Frontend

| 파일 | 변경 내용 | 추가 라인 |
|------|----------|----------|
| `frontend/src/services/api.js` | 새 API 함수 추가 | ~75 lines |
| `frontend/src/components/Dashboard.js` | 새 버튼/모달 통합 | 전체 수정 |
| `frontend/src/components/StatsPanel.js` | **신규** | 305 lines |
| `frontend/src/components/ReplyHistory.js` | **신규** | 169 lines |
| `frontend/src/components/ReplyGenerator.js` | 피드백 학습 호출 추가 | +15 lines |
| `frontend/src/styles/App.css` | 새 컴포넌트 스타일 | ~640 lines |

---

## 4. 테스트 결과

### 4.1 Backend API 테스트

| Endpoint | Method | Status | 결과 |
|----------|--------|--------|------|
| `/stats/overview` | GET | `200 OK` | ✅ PASS |
| `/stats/reply-history` | GET | `200 OK` | ✅ PASS |
| `/feedback/learn` | POST | `200 OK` | ✅ PASS |
| `/feedback/stats` | GET | `200 OK` | ✅ PASS |

### 4.2 Frontend UI 테스트

| 컴포넌트 | 테스트 항목 | 결과 |
|----------|------------|------|
| Dashboard | 새 버튼 렌더링 | ✅ PASS |
| StatsPanel | 개요 탭 | ✅ PASS |
| StatsPanel | 분포 탭 | ✅ PASS |
| StatsPanel | 학습 현황 탭 | ✅ PASS |
| ReplyHistory | 목록 표시 | ✅ PASS |
| ReplyHistory | 상세 모달 | ✅ PASS |

### 4.3 통합 테스트

```
전체 테스트 결과: ✅ ALL PASS
- Backend API: 4/4 (100%)
- Frontend UI: 7/7 (100%)
- 빌드 상태: 성공 (ESLint 경고 2건, 기능 무관)
```

---

## 5. 향후 개선 방향

### 5.1 피드백 학습 고도화
- [ ] 피드백 기반 Few-shot Learning 적용
- [ ] 사용자별 선호 톤 자동 추천
- [ ] 피드백 품질 점수화 및 가중치 적용

### 5.2 UI/UX 개선
- [ ] 실시간 알림 시스템 (새 이메일, 답변 필요)
- [ ] 다크 모드 지원
- [ ] 모바일 반응형 디자인

### 5.3 분석 기능 확장
- [ ] 답변 시간 분석 (평균 응답 시간)
- [ ] 발신자별 통계
- [ ] 주간/월간 리포트 자동 생성

---

## 6. 결론

Phase 5 고도화를 통해 다음을 달성하였습니다:

1. **지속 학습 능력 확보**: ChromaDB 기반 피드백 저장 및 검색 시스템 구축으로 AI가 사용자의 답변 스타일을 학습할 수 있는 기반 마련

2. **사용자 경험 향상**: 통계 대시보드와 답변 히스토리를 통해 시스템 활용도 및 AI 학습 현황을 시각적으로 확인 가능

3. **확장 가능한 아키텍처**: ChromaDB Collection 기반 설계로 향후 다양한 피드백 유형 및 학습 알고리즘 적용 용이

---

**작성일**: 2025-12-17
**버전**: Phase 5 (v1.0)
**테스트 환경**: Docker (mail_backend, mail_frontend, mail_postgres)
